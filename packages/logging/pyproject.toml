[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eventmesh-logging"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ['structlog>=24.1']

[tool.setuptools.packages.find]
include = ["eventmesh_logging*"]
