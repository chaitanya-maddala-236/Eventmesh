[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eventmesh-metrics"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ['prometheus-client>=0.20']

[tool.setuptools.packages.find]
include = ["eventmesh_metrics*"]
